<p>Hi security team,</p>

<p>This is your weekly recap of all open security issues. Let's work on closing these issues!</p>

<table>
    {% for item in results %}
    <tr>
        <td align="right"><a href="{{ item.issue_url }}">{{ item.issue }}</a></td>
        <td colspan=2>{{ item.summary }} </td>
    </tr><tr>
        <td align="right">{{ item.priority }}</td>
        <td>{{ item.status }}</td>
        <td>Assigned to: {{ item.assignee }}</td>
    </tr>
    <tr></tr>
    {% endfor %}
 </table>

<p>Thanks!</p>